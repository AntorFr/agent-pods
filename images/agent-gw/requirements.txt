fastapi>=0.115
uvicorn[standard]>=0.30
# Required by FastAPI to parse multipart bodies (chat file attachments).
python-multipart>=0.0.9
# >=0.2.124 : première version où j'ai vérifié le champ max_buffer_size
# (ClaudeAgentOptions) — nécessaire pour lire une image jointe sans déborder
# le buffer stdout de 1 Mo du SDK.
claude-agent-sdk>=0.2.124
authlib>=1.3
itsdangerous>=2.2
httpx>=0.27
# MCP server SDK — Alfred exposes an /mcp endpoint so other agents can call him.
#
# ⚠️ PLAFONNÉ À 1.x, et ce n'est pas de la prudence de principe. Ce plafond était tenu
# PAR ACCIDENT : `workspace-mcp==1.22.0` (retiré le 2026-08-19) épinglait `mcp` en 1.x
# par transitivité. Sans lui, `mcp>=1.2` a résolu en 2.0.0 — qui a supprimé
# `mcp.server.fastmcp`, importé par `app/main.py`. La gateway ne démarrait plus du tout.
#
# Pris à la construction de l'image, pas en production, mais de justesse : la CI de ce
# dépôt BUILDE sans rien exécuter, donc rien ne l'aurait vu partir. Monter en 2.x est un
# vrai chantier (le module a été renommé), à faire délibérément — pas par résolution de
# dépendances un jour de build.
mcp>=1.2,<2
