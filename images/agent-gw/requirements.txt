fastapi>=0.115
uvicorn[standard]>=0.30
claude-agent-sdk>=0.1
authlib>=1.3
itsdangerous>=2.2
httpx>=0.27
# Google Workspace MCP server (stdio), spawned by the agent harness when the
# workspace .mcp.json asks for it. Pinned: an MCP server is a security surface.
workspace-mcp==1.22.0
# MCP server SDK — Alfred exposes an /mcp endpoint so other agents can call him.
mcp>=1.2
